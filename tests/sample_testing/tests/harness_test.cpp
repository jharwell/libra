// Stub test harness — must compile as a static library source.
// testing.cmake globs this for LIBRA_c/cxx_test_harness via LIBRA_TEST_HARNESS_MATCHER.
namespace harness { int init() { return 0; } }
