name: Build and test

# Run:
#
# - Every month. Scheduling helps keep drift/issues with updates
#   to runners at bay.
# - On pushes to relevant paths.
#
# Ignore tags. On both devel and master they are redundant, because CI would
# have previously succeeded for both on a branch commit (automatically for devel
# tags).
#
on:
  push:
    tags-ignore:
      - '**'
    paths:
      - "cmake/**"
      - "cli/**"
      - ".github/**"
      - "docs/**"

  schedule:
    - cron: '0 0 1 * *'


concurrency:
  group: "build-and-test-${{ github.ref }}"
  cancel-in-progress: true

jobs:
  cmake-funcs-smoke-tests:
    runs-on: ubuntu-latest
    container:
      image: jharwell/ubuntu24.04-libra:2026-03-16
    steps:
      - uses: actions/checkout@v4
      - name: Run smoke tests
        shell: bash
        run: |
          cd tests && mkdir -p build && cd build
          cmake ..
          ctest --output-on-failure -j $(nproc)


  cmake-add-subdirectory-smoke-tests:
    uses: ./.github/workflows/smoke-tests-compiler-matrix.yml
    with:
      suite-script: ./suites/run_suite_add_subdirectory.sh
      full-matrix: ${{ github.ref_name == 'devel' || github.ref_name == 'master' || github.event_name == 'schedule' }}

  cmake-installed-pkg-smoke-tests:
    uses: ./.github/workflows/smoke-tests-compiler-matrix.yml
    with:
      suite-script: ./suites/run_suite_installed_package.sh
      full-matrix: ${{ github.ref_name == 'devel' || github.ref_name == 'master' || github.event_name == 'schedule' }}

  cmake-cpm-smoke-tests:
    uses: ./.github/workflows/smoke-tests-compiler-matrix.yml
    with:
      suite-script: ./suites/run_suite_cpm.sh
      full-matrix: ${{ github.ref_name == 'devel' || github.ref_name == 'master' || github.event_name == 'schedule' }}

  cmake-conan-smoke-tests:
    uses: ./.github/workflows/smoke-tests-compiler-matrix.yml
    with:
      suite-script: ./suites/run_suite_conan.sh
      pip-install-conan: true
      full-matrix: ${{ github.ref_name == 'devel' || github.ref_name == 'master' || github.event_name == 'schedule' }}

  schema-help-targets:
    runs-on: ubuntu-latest
    container:
      image: jharwell/ubuntu24.04-libra:2026-03-16
    steps:
      - uses: actions/checkout@v4
      - name: Install uv
        uses: astral-sh/setup-uv@v6
      - name: Run LIBRA install
        shell: bash
        run: |
          cd tests/sample_basic
          cmake .
          uv run check-jsonschema --schemafile ../../schema/help-targets.yaml libra_targets.json


  cmake-self-install-test:
    runs-on: ubuntu-latest
    container:
      image: jharwell/ubuntu24.04-libra:2026-03-16
    steps:
      - uses: actions/checkout@v4
      - name: Run LIBRA install
        shell: bash
        run: |
          mkdir -p build && cd build
          cmake ..
          make install

  cli-build:
    runs-on: ubuntu-latest
    container:
      image: jharwell/ubuntu24.04-libra:2026-03-16
    steps:
      - uses: actions/checkout@v4
      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Build CLI
        run: cargo build
        working-directory: .

  cli-unit-tests:
    runs-on: ubuntu-latest
    container:
      image: jharwell/ubuntu24.04-libra:2026-03-16
    needs:
      - cli-build
    steps:
      - uses: actions/checkout@v4
      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
      - name: Run Rust unit tests
        run: cargo test
        working-directory: .

  cli-smoke-tests:
    runs-on: ubuntu-latest
    container:
      image: jharwell/ubuntu24.04-libra:2026-03-16
    needs:
      - cli-build
    steps:
      - uses: actions/checkout@v4
      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Run CLI BATS smoke tests
        shell: bash
        run: |
          cargo build
          bats tests/cli_*.bats -j $(nproc)
        working-directory: ${{ github.workspace }}
        env:
          CLIBRA_BIN: ${{ github.workspace }}/target/debug/clibra

  docs:
    if: github.ref_name == 'master'
    uses: ./.github/workflows/pages.yml
    needs:
      - cmake-funcs-smoke-tests
      - cmake-add-subdirectory-smoke-tests
      - cmake-cpm-smoke-tests
      - cmake-conan-smoke-tests
      - cmake-installed-pkg-smoke-tests
      - cmake-self-install-test
      - cli-smoke-tests
      - cli-unit-tests
      - schema-help-targets
    secrets: inherit

  create-conan-pkg:
    # Only publish packages from master
    if: github.ref_name == 'master'
    runs-on: ubuntu-latest
    needs:
      - cmake-funcs-smoke-tests
      - cmake-add-subdirectory-smoke-tests
      - cmake-cpm-smoke-tests
      - cmake-conan-smoke-tests
      - cmake-installed-pkg-smoke-tests
      - cmake-self-install-test
      - cli-smoke-tests
      - cli-unit-tests
    steps:
      - uses: actions/checkout@v4
      - name: Create package
        shell: bash
        run: |
          pip3 install conan
          conan profile detect
          conan create \
            --user jharwell \
            --lockfile-out=package.lock \
            .

  tag-dev-release:
    if: github.ref_type == 'branch' && github.ref_name == 'devel'
    runs-on: ubuntu-latest
    container:
      image: jharwell/ubuntu24.04-libra:2026-03-16
    permissions:
      contents: write
    needs:
      - cmake-funcs-smoke-tests
      - cmake-add-subdirectory-smoke-tests
      - cmake-cpm-smoke-tests
      - cmake-conan-smoke-tests
      - cmake-installed-pkg-smoke-tests
      - cmake-self-install-test
      - cli-smoke-tests
      - cli-unit-tests
      - schema-help-targets
    steps:
      - uses: actions/checkout@v4
        with:
          # Full history required to inspect all existing tags.
          fetch-depth: 0
          # Push with GITHUB_TOKEN (the default), NOT a PAT. GITHUB_TOKEN-made
          # pushes cannot trigger further workflow runs -- GitHub's built-in
          # loop protection -- so the self.cmake commit below re-triggering
          # this very workflow (which fires on push to cmake/**) is structurally
          # impossible. Dev tagging has no downstream workflow to fire, so it
          # does not need the release PAT. [skip ci] on the commit is a second,
          # independent guard. Do NOT switch this to BOT_RELEASE_TOKEN.
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Configure git
        run: |
          git config --global --add safe.directory "${GITHUB_WORKSPACE}"
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Compute next dev tag
        id: tag
        shell: bash
        run: |
          git fetch --tags
          NEW_VERSION=$(./scripts/bump-version.sh)
          echo "New dev version: ${NEW_VERSION}"
          echo "new_version=${NEW_VERSION}" >> $GITHUB_OUTPUT

      - name: Bake version into self.cmake
        shell: bash
        run: |
          VERSION="${{ steps.tag.outputs.new_version }}"
          # Strip leading 'v' for the CMake set() call
          VERSION="${VERSION#v}"

          # Quoted delimiter prevents shell/YAML expansion inside the heredoc;
          # the version is substituted afterward with sed. self.cmake is the
          # git-less fallback that CPM consumers (which fetch a git-less tree at
          # the tag) read at configure time to report LIBRA_VERSION.
          cat > cmake/libra/self.cmake << 'CMAKEEOF'
          # GENERATED FILE -- do not edit.
          # Updated automatically by CI in the tag-dev-release job.
          # Any manual changes will be overwritten on the next devel merge.
          set(LIBRA_VERSION "__VERSION__")
          CMAKEEOF
          sed -i "s/__VERSION__/${VERSION}/" cmake/libra/self.cmake

      - name: Commit and tag
        shell: bash
        run: |
          NEW_VERSION="${{ steps.tag.outputs.new_version }}"

          git add cmake/libra/self.cmake
          # [skip ci] is REQUIRED: this commit lands on devel and touches
          # cmake/**, which is one of this workflow's push-trigger paths.
          # Without the token, [skip ci] guarantees the version-stamp commit
          # cannot re-enter CI. It changed only generated metadata, so skipping
          # CI is also semantically correct -- there is no source to test.
          git commit -m "chore: update LIBRA_VERSION to ${NEW_VERSION} [skip ci]"

          # Tag the new commit, not the pre-commit HEAD
          git tag "${NEW_VERSION}"

          # Push the version bump commit to devel and the tag together.
          # Pushed with GITHUB_TOKEN (see checkout above) -> no workflow re-trigger.
          git push origin HEAD:devel "${NEW_VERSION}"
