@PACKAGE_INIT@

# 2025-10-13 [JRH]: This makes LIBRA available via find_package() once
# installed, assuming it is installed to CMAKE_INSTALL_PREFIX, or otherwise the
# necessary prefix is added to CMAKE_PREFIX_PATH
list(APPEND CMAKE_MODULE_PATH "${CMAKE_CURRENT_LIST_DIR}")

check_required_components(libra)
